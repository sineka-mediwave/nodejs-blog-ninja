// it gets data from the module people
// const data = require("./people");
const { people, ages } = require("./people");

console.log(people, ages);

const os = require("os");
console.log(os.platform);
